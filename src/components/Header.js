function Header(props) {
    return (
        <header>
            <h3>Logo</h3>
            <nav>
                <button>Home</button>
                <button>About</button>
            </nav>
        </header>
    )
}

export default Header;